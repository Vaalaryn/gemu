import {StyleSheet} from "react-native";

export const styleGameDetail = StyleSheet.create({
    container: {
        flexDirection: 'column',
        justifyContent: 'flex-start',
        flex: 1,
        top: 0
    },
    element: {
    }
});